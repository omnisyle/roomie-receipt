import React from "react";

/*
 * Add receipt page
 * should display a form to allow user to add a receipt
 * the receipt should contains the following data
 * - Date - Date picker
 * - Vendor - Text field
 * - Total - Number field
 * - Buyer - select list
 */
class AddReceipt extends React.Component {

  render() {

    return (
      <div className="add-receipt-container">
      </div>
    );
  }
}

export default AddReceipt;
